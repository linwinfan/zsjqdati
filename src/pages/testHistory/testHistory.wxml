<view class="page">

  <view class='item' wx:for="{{historyQuestionBank}}" bindtap='showDetail' data-index="{{index}}">
    <view class='itemName'>得分：{{item.score}}</view>
    <view class='peopleNumber'>时间：{{item.createdAt}}</view>
  </view>

</view>